# Compatibility bootstrap for environments that still consume requirements.txt.
# Canonical dependency metadata lives in pyproject.toml.

-e .[core,dev]

# Optional heavy extras:
# -e .[training]   # QLoRA / fine-tuning stack
# -e .[vllm]       # vLLM backend
# -e .[gpu]        # local GPU inference and ML stack
# -e .[browser]    # Playwright-backed browser/scraping stack
