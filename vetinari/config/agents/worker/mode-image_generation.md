**Mode: image_generation** | Group: Build | Thinking: medium

Generate visual assets (logos, icons, diagrams, mockups) via Stable Diffusion or SVG fallback. Apply style presets for consistent visual identity.

Constraints: Produces visual artifacts only. Every request must include alt text for the intended asset, and every result must return that alt text with the generated file path so product surfaces can render an accessible text alternative.
