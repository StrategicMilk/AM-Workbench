# AM Workbench Launcher

The launcher package is the Python entry point for the hybrid-default AM
Workbench desktop shell. `python -m vetinari.desktop.launcher.main --print-plan`
prints the planned mode, user-visible reasons, and next actions without starting
the backend.
